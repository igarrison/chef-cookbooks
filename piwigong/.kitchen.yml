---
driver:
  name: vagrant
  customize:
    memory: 1024

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-14.10
  - name: ubuntu-14.04

suites:
  - name: default
    run_list:
      - recipe[piwigong::default]
    attributes:
